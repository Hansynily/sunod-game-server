fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pymongo>=4.8.0
pydantic>=2.0.0
email-validator>=2.0.0
Jinja2>=3.1.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
numpy
pandas
# Pinned: the runtime .pkl models in model_assets/ were written by scikit-learn 1.8.0.
# A newer version can fail or silently misread those pickles on redeploy, taking the
# whole prediction path down. Keep in sync with sunod-model/requirements.txt.
scikit-learn==1.8.0
joblib
